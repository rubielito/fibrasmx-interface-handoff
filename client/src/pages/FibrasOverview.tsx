/* FIBRAS.MX — Dashboard y Portafolio educativo. Esta vista registra referencias manuales; no ejecuta órdenes ni recomienda compras. */
import { useEffect, useMemo, useState } from "react";
import * as XLSX from "xlsx";
import { ArrowLeft, Bell, BarChart3, BriefcaseBusiness, CalendarDays, Check, FileImage, Heart, LogOut, Pencil, Plus, ReceiptText, Trash2, TrendingDown, TrendingUp, Trophy, Upload, X } from "lucide-react";
import { useLocalAuth } from "@/contexts/LocalAuthContext";
import { getScopedStorageKey } from "@/lib/localAuth";

const rankings = [
  { title: "Top Rendimientos", icon: TrendingUp, tone: "up", rows: [["FUNO", "Fibra Uno", "+1.24%"], ["FIBRAPL", "Prologis", "+1.15%"], ["FHIPO", "FHipo", "+1.01%"], ["FMTY", "Fibra Mty", "+0.57%"], ["DANHOS", "Danhos", "+0.35%"]] },
  { title: "Mayores Caídas", icon: TrendingDown, tone: "down", rows: [["FIHO", "FibraHotel", "−1.57%"], ["FMX", "Fibra MX", "−0.96%"], ["FIBRAMQ", "Macquarie", "−0.81%"], ["FSHOP", "FShop", "−0.50%"], ["FCFE", "Fibra CFE", "−0.12%"]] },
  { title: "Mayor Volumen", icon: BarChart3, tone: "neutral", rows: [["FMTY", "Fibra Mty", "3,133,214"], ["FUNO", "FUNO", "2,167,228"], ["FIBRAPL", "Prologis", "649,572"], ["FIBRAMQ", "Macquarie", "494,165"], ["DANHOS", "Danhos", "182,390"]] },
  { title: "Mayor Capitalización", icon: Trophy, tone: "neutral", rows: [["FIBRAPL", "Prologis", "$124.3B"], ["FUNO", "FUNO", "$112.0B"], ["FPLUS", "FPlus", "$111.3B"], ["FHIPO", "FHipo", "$67.3B"], ["EDUCA", "Educa", "$66.3B"]] },
];

function ToolHeader({ active }: { active: "dashboard" | "portafolio" }) { return <header className="mx-header"><a href="/" className="mx-logo"><span />FIBRAS<span>.MX</span></a><nav className="mx-nav"><a href="/" className="mx-home-link">Inicio</a><a href="/fibras-mx"><BarChart3 size={14} />FIBRAs</a><a className={active === "dashboard" ? "active" : ""} href="/fibras-mx/dashboard">Dashboard</a><a href="/fibras-mx/comparador">Comparador</a><a href="/fibras-mx/calculadora">Calculadora</a><a className={active === "portafolio" ? "active" : ""} href="/fibras-mx/portafolio">Portafolio</a></nav><div className="mx-market"><i /> BMV · México</div></header>; }
function Footer() { return <footer className="mx-footer"><span>FIBRAS<span>.MX</span> — Centro educativo</span><span>Datos de referencia · Información sin fines de asesoría</span></footer>; }

export function Dashboard() { return <div className="mx-app"><ToolHeader active="dashboard" /><main className="mx-tool-main"><p className="mx-kicker">Lectura de mercado · Referencia educativa</p><h1 className="mx-tool-title">Dashboard</h1><div className="mx-rank-grid">{rankings.map(({ title, icon: Icon, tone, rows }) => <section className="mx-rank-panel" key={title}><h2><Icon size={15} className={tone} />{title}</h2>{rows.map(([ticker, name, value]) => <div className="mx-rank-row" key={ticker}><span><b>{ticker}</b><small>{name}</small></span><strong className={tone}>{value}</strong></div>)}</section>)}</div><section className="mx-empty-panel"><Heart size={16} /><div><h2>Mis Favoritos</h2><p>No tienes FIBRAs favoritas aún. Ve a la página principal para añadir instrumentos a tu lista educativa.</p></div></section><section className="mx-empty-panel"><Bell size={16} /><div><h2>Mis Alertas de Precio</h2><p>No tienes alertas activas. Ve al detalle de una FIBRA para crear una alerta de seguimiento.</p></div></section><p className="mx-disclaimer">Las cifras son referencias de demostración y no constituyen asesoría financiera ni recomendación de compra o venta.</p></main><Footer /></div>; }

const portfolioOptions = ["FUNO", "FIBRAPL", "FIBRAMQ", "DANHOS", "FSHOP", "FINN", "FIHO", "FMTY", "FPLUS", "NEXT", "FNOVA", "FHIPO", "EDUCA", "STORAGE", "FCFE", "TERRA", "FMX", "SOMA", "UPSITE"];
const followingCatalog: Record<string, string> = { FUNO: "Fibra Uno", FIBRAPL: "Fibra Prologis", FIBRAMQ: "Fibra Macquarie", DANHOS: "Danhos", FSHOP: "Fibra Shop", FINN: "Fibra Inn", FIHO: "FibraHotel", FMTY: "Fibra Mty", FPLUS: "Fibra Plus", NEXT: "Fibra Next", FNOVA: "Fibra Nova", FHIPO: "FHipo", EDUCA: "Fibra Educa", STORAGE: "Fibra Storage", FCFE: "Fibra CFE", TERRA: "Terrafina", FMX: "Fibra MX", SOMA: "Fibra Soma", UPSITE: "Fibra Upsite" };
const followingMarket: Record<string, { price: string; change: string; yield: string }> = { FUNO: { price: "$29.35", change: "+1.24%", yield: "7.88%" }, FIBRAPL: { price: "$74.51", change: "+1.15%", yield: "5.21%" }, FIBRAMQ: { price: "$42.65", change: "−0.81%", yield: "5.43%" }, DANHOS: { price: "$28.45", change: "+0.35%", yield: "6.43%" }, FSHOP: { price: "$11.95", change: "−0.50%", yield: "—" }, FINN: { price: "$4.80", change: "+0.21%", yield: "7.48%" }, FIHO: { price: "$7.54", change: "−1.57%", yield: "7.96%" }, FMTY: { price: "$14.22", change: "+0.57%", yield: "3.45%" }, FPLUS: { price: "$5.07", change: "0.00%", yield: "1.70%" }, NEXT: { price: "$90.50", change: "−0.66%", yield: "2.50%" }, FNOVA: { price: "$41.72", change: "+0.14%", yield: "5.65%" }, FHIPO: { price: "$14.00", change: "+1.01%", yield: "10.14%" }, EDUCA: { price: "$54.00", change: "0.00%", yield: "4.74%" }, STORAGE: { price: "$23.87", change: "0.00%", yield: "10.12%" }, FCFE: { price: "$24.73", change: "−0.12%", yield: "8.49%" }, TERRA: { price: "$1.20", change: "—", yield: "—" }, FMX: { price: "$30.01", change: "−0.96%", yield: "1.43%" }, SOMA: { price: "$18.90", change: "—", yield: "—" }, UPSITE: { price: "$31.50", change: "—", yield: "—" } };
type Movement = "Compra" | "Venta" | "Distribución" | "Ajuste";
type PortfolioRecord = { id: string; ticker: string; movement: Movement; date: string; shares: number; price: number; currentPrice: number; amount: number; commission: number; broker: string; sourceStatus: "Pendiente" | "Confirmado"; folio: string; receiptName: string; receiptData?: string; notes: string; };
const emptyDraft: Omit<PortfolioRecord, "id"> = { ticker: "FUNO", movement: "Compra", date: new Date().toISOString().slice(0, 10), shares: 0, price: 0, currentPrice: 0, amount: 0, commission: 0, broker: "", sourceStatus: "Pendiente", folio: "", receiptName: "", receiptData: "", notes: "" };

export function Portfolio() {
  const { nickname, logout, requestLogin } = useLocalAuth();
  const portfolioKey = getScopedStorageKey("fibras-mx-portfolio", nickname);
  const followingKey = getScopedStorageKey("fibras-mx-following", nickname);
  const [records, setRecords] = useState<PortfolioRecord[]>([]);
  const [draft, setDraft] = useState<Omit<PortfolioRecord, "id">>(emptyDraft);
  const [editingId, setEditingId] = useState<string | null>(null);
  const [showForm, setShowForm] = useState(false);
  const [saved, setSaved] = useState(false);
  const [importRows, setImportRows] = useState<Omit<PortfolioRecord, "id">[]>([]);
  const [importError, setImportError] = useState("");
  const [following, setFollowing] = useState<string[]>(() => { try { return JSON.parse(window.localStorage.getItem(followingKey) || "[]"); } catch { return []; } });

  useEffect(() => { try { const stored = localStorage.getItem(portfolioKey); if (stored) setRecords((JSON.parse(stored) as Partial<PortfolioRecord>[]).map(item => ({ ...emptyDraft, ...item, sourceStatus: item.sourceStatus === "Confirmado" ? "Confirmado" : "Pendiente" } as PortfolioRecord))); else setRecords([]); } catch { setRecords([]); } }, [portfolioKey]);
  useEffect(() => { localStorage.setItem(portfolioKey, JSON.stringify(records)); }, [portfolioKey, records]);
  useEffect(() => { const syncFollowing = () => { try { setFollowing(JSON.parse(window.localStorage.getItem(followingKey) || "[]")); } catch { setFollowing([]); } }; syncFollowing(); window.addEventListener("storage", syncFollowing); return () => window.removeEventListener("storage", syncFollowing); }, [followingKey]);

  const totalReference = useMemo(() => records.reduce((sum, item) => sum + (item.movement === "Compra" ? item.shares * item.price + (item.commission || 0) : item.movement === "Venta" ? -(item.shares * item.price + (item.commission || 0)) : item.amount), 0), [records]);
  const currentValue = useMemo(() => records.reduce((sum, item) => sum + (item.movement === "Compra" ? item.shares * (item.currentPrice || item.price) : 0), 0), [records]);
  const gainLoss = currentValue - totalReference;
  const totalShares = useMemo(() => records.reduce((sum, item) => sum + (item.movement === "Compra" ? item.shares : item.movement === "Venta" ? -item.shares : 0), 0), [records]);
  const distributions = useMemo(() => records.filter(item => item.movement === "Distribución").reduce((sum, item) => sum + item.amount, 0), [records]);
  const fibraStats = useMemo(() => Array.from(new Set(records.map(item => item.ticker))).map(ticker => { const rows = records.filter(item => item.ticker === ticker); const shares = rows.reduce((sum, item) => sum + (item.movement === "Compra" ? item.shares : item.movement === "Venta" ? -item.shares : 0), 0); const invested = rows.reduce((sum, item) => sum + (item.movement === "Compra" ? item.shares * item.price + (item.commission || 0) : item.movement === "Venta" ? -(item.shares * item.price + (item.commission || 0)) : 0), 0); const paid = rows.filter(item => item.movement === "Distribución").reduce((sum, item) => sum + item.amount, 0); return { ticker, shares, invested, paid, rows: rows.length }; }), [records]);
  const maxInvested = Math.max(...fibraStats.map(item => item.invested), 0);
  const totalInvested = fibraStats.reduce((sum, item) => sum + item.invested, 0);
  const sortedDistributions = useMemo(() => records.filter(item => item.movement === "Distribución").sort((a, b) => a.date.localeCompare(b.date)), [records]);
  const distributionPoints = useMemo(() => { let cumulative = 0; return sortedDistributions.map((row, index) => { cumulative += row.amount; return { ...row, cumulative, index }; }); }, [sortedDistributions]);
  const maxDistribution = Math.max(...distributionPoints.map(item => item.cumulative), 0);
  const participationGradient = useMemo(() => { if (!totalInvested) return "conic-gradient(#2a2a2d 0 100%)"; const colors = ["#ef1735", "#f3a712", "#50c878", "#4b7bec", "#9b59b6"]; let cursor = 0; return `conic-gradient(${fibraStats.map((item, index) => { const start = cursor; cursor += (item.invested / totalInvested) * 100; return `${colors[index % colors.length]} ${start}% ${cursor}%`; }).join(", ")})`; }, [fibraStats, totalInvested]);

  const updateDraft = (field: keyof Omit<PortfolioRecord, "id">, value: string | number) => setDraft(current => ({ ...current, [field]: value }));
  const handleReceipt = (file?: File) => { if (!file) return; const reader = new FileReader(); reader.onload = () => setDraft(current => ({ ...current, receiptName: file.name, receiptData: typeof reader.result === "string" ? reader.result : "" })); reader.readAsDataURL(file); };
  const resetForm = () => { setDraft({ ...emptyDraft, date: new Date().toISOString().slice(0, 10) }); setEditingId(null); setShowForm(false); setSaved(false); };
  const saveRecord = () => { if (!draft.ticker || !draft.date) return; const record = { ...draft, shares: Number(draft.shares) || 0, price: Number(draft.price) || 0, currentPrice: Number(draft.currentPrice) || 0, amount: Number(draft.amount) || 0, commission: Number(draft.commission) || 0 }; setRecords(current => editingId ? current.map(item => item.id === editingId ? { ...record, id: editingId } : item) : [{ ...record, id: `${Date.now()}-${draft.ticker}` }, ...current]); setSaved(true); setTimeout(() => { setSaved(false); resetForm(); }, 700); };
  const normalizeImportRow = (row: Record<string, unknown>) => { const normalized = Object.fromEntries(Object.entries(row).map(([key, value]) => [key.toLowerCase().replace(/[áéíóúüñ\s_-]/g, character => ({ "á": "a", "é": "e", "í": "i", "ó": "o", "ú": "u", "ü": "u", "ñ": "n", " ": "", "_": "", "-": "" }[character] || "")), value])); const ticker = String(normalized.ticker || normalized.fibra || normalized.emisora || "").toUpperCase().trim(); const movementValue = String(normalized.movement || normalized.movimiento || "Compra").toLowerCase(); const movement: Movement = movementValue.includes("distrib") || movementValue.includes("dividend") ? "Distribución" : movementValue.includes("ajust") ? "Ajuste" : movementValue.includes("venta") || movementValue.includes("sell") ? "Venta" : "Compra"; const date = String(normalized.date || normalized.fecha || new Date().toISOString().slice(0, 10)).slice(0, 10); const shares = Number(normalized.shares || normalized.titulos || normalized.títulos || 0) || 0; const price = Number(normalized.price || normalized.precio || normalized.costopromedio || 0) || 0; const currentPrice = Number(normalized.currentprice || normalized.precioactual || normalized.precioobservado || 0) || 0; const amount = Number(normalized.amount || normalized.monto || normalized.importe || 0) || 0; const commission = Number(normalized.commission || normalized.comision || normalized.comisiones || 0) || 0; const sourceStatus: PortfolioRecord["sourceStatus"] = String(normalized.sourcestatus || normalized.estado || "Pendiente").toLowerCase().includes("confirm") ? "Confirmado" : "Pendiente"; return { ...emptyDraft, ticker: portfolioOptions.includes(ticker) ? ticker : "FUNO", movement, date, shares, price, currentPrice, amount, commission, broker: String(normalized.broker || normalized.intermediario || normalized.casa || ""), sourceStatus, folio: String(normalized.folio || normalized.referencia || ""), notes: String(normalized.notes || normalized.notas || "") }; };
  const handleImportFile = (file?: File) => { if (!file) return; setImportError(""); const reader = new FileReader(); reader.onload = event => { try { const data = event.target?.result; const workbook = file.name.toLowerCase().endsWith(".csv") ? XLSX.read(String(data), { type: "string" }) : XLSX.read(data, { type: "array" }); const sheet = workbook.Sheets[workbook.SheetNames[0]]; const rows = XLSX.utils.sheet_to_json<Record<string, unknown>>(sheet, { defval: "" }).map(normalizeImportRow).filter(row => row.ticker && row.date); setImportRows(rows); if (!rows.length) setImportError("No se encontraron filas válidas. Revisa las columnas ticker, movimiento y fecha."); } catch { setImportRows([]); setImportError("No fue posible leer el archivo. Usa XLSX o CSV y confirma sus datos antes de guardarlos."); } }; if (file.name.toLowerCase().endsWith(".csv")) reader.readAsText(file); else reader.readAsArrayBuffer(file); };
  const confirmImport = () => { if (!importRows.length) return; setRecords(current => [...importRows.map(row => ({ ...row, id: `${Date.now()}-${row.ticker}-${Math.random().toString(36).slice(2, 7)}` })), ...current]); setImportRows([]); setImportError(""); };
  const openForm = (movement: Movement = "Compra") => { if (!nickname) { requestLogin(); return; } setEditingId(null); setDraft({ ...emptyDraft, movement, date: new Date().toISOString().slice(0, 10) }); setShowForm(true); window.setTimeout(() => document.getElementById("portfolio-form")?.scrollIntoView({ behavior: "smooth", block: "start" }), 80); };
  const editRecord = (record: PortfolioRecord) => { const { id, ...rest } = record; setDraft(rest); setEditingId(id); setShowForm(true); window.setTimeout(() => document.getElementById("portfolio-form")?.scrollIntoView({ behavior: "smooth", block: "start" }), 80); };
  const deleteRecord = (id: string) => { if (window.confirm("¿Eliminar este registro educativo del portafolio?")) setRecords(current => current.filter(item => item.id !== id)); };

  return <div className="mx-app"><ToolHeader active="portafolio" /><main className="mx-tool-main mx-portfolio-page"><p className="mx-kicker">Seguimiento personal · Modo educativo · Solo FIBRAs mexicanas</p><div className="mx-portfolio-title-row"><div><h1 className="mx-tool-title">Mi portafolio</h1><p className="mx-tool-subtitle">Registra manualmente tus movimientos para estudiar tu historial. No se conecta a GBM ni ejecuta órdenes.</p><span className="mx-local-user-badge">Sesión local: <strong>{nickname}</strong> <button type="button" onClick={logout}><LogOut size={12} />Cerrar sesión</button></span></div><div className="mx-portfolio-quick-actions"><button className="mx-add-button mx-primary-action" onClick={() => openForm("Compra")}><Plus size={15} />Agregar posición</button><button className="mx-transaction-button mx-buy-action" onClick={() => openForm("Compra")}>🟢 Compra</button><button className="mx-transaction-button mx-sell-action" onClick={() => openForm("Venta")}>🔴 Venta</button></div></div>
    <div className="mx-portfolio-summary"><div><span>Registros</span><strong>{records.length}</strong><small>capturados manualmente</small></div><div><span>FIBRAs estudiadas</span><strong>{new Set(records.map(item => item.ticker)).size}</strong><small>sin recomendación</small></div><div><span>Costo de referencia</span><strong>${totalReference.toLocaleString("es-MX", { maximumFractionDigits: 2 })}</strong><small>incluye comisiones</small></div><div><span>Valor observado</span><strong>${currentValue.toLocaleString("es-MX", { maximumFractionDigits: 2 })}</strong><small className={gainLoss >= 0 ? "mx-positive" : "mx-negative"}>{gainLoss >= 0 ? "+" : ""}${gainLoss.toLocaleString("es-MX", { maximumFractionDigits: 2 })} vs. costo</small></div></div>
    <section className="mx-portfolio-overview"><div className="mx-portfolio-welcome"><div><span className="mx-portfolio-section-label"><BarChart3 size={14} />Resumen del portafolio</span><h2>Sigue así, paso a paso.</h2><p>Cuando agregues tus registros podrás observar el valor acumulado, las distribuciones y la participación de cada FIBRA en tu historial educativo.</p></div><div className="mx-portfolio-welcome-mark"><BriefcaseBusiness size={34} /></div><div className="mx-portfolio-welcome-stats"><div><span>Invertido</span><strong>${totalReference.toLocaleString("es-MX", { maximumFractionDigits: 0 })}</strong><small>referencia</small></div><div><span>Distribuciones</span><strong>${distributions.toLocaleString("es-MX", { maximumFractionDigits: 0 })}</strong><small>registradas</small></div></div></div><div className="mx-portfolio-overview-grid"><section className="mx-portfolio-chart"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label">Evolución</span><h2>Rendimientos · últimos 6 meses</h2></div><span>Sin datos aún</span></div>{fibraStats.length === 0 ? <div className="mx-portfolio-empty-chart"><BarChart3 size={24} /><span>Agrega compras para generar la gráfica</span></div> : <div className="mx-bar-chart">{fibraStats.map(item => <div className="mx-bar-item" key={item.ticker}><div className="mx-bar-track"><span style={{ height: `${maxInvested ? Math.max(8, (item.invested / maxInvested) * 100) : 8}%` }} /></div><strong>{item.ticker}</strong><small>${item.invested.toLocaleString("es-MX", { maximumFractionDigits: 0 })}</small></div>)}</div>}</section><section className="mx-portfolio-participation"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label">Distribución</span><h2>Participación en portafolio</h2></div><span>FIBRAs</span></div>{records.length === 0 ? <div className="mx-portfolio-empty-list"><span>Sin FIBRAs registradas</span><small>El círculo se llenará con las compras manuales.</small></div> : <div className="mx-donut-layout"><div className="mx-donut" style={{ background: participationGradient }}><div><strong>{fibraStats.length}</strong><span>FIBRAs</span></div></div><div className="mx-donut-legend">{fibraStats.map(item => <div key={item.ticker}><i style={{ background: participationGradient }} /><strong>{item.ticker}</strong><span>{totalInvested ? `${((item.invested / totalInvested) * 100).toFixed(1)}%` : "0%"}</span></div>)}</div></div>}</section></div><section className="mx-distribution-card"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label">Distribuciones</span><h2>Acumulado por fecha</h2></div><span>{sortedDistributions.length} registros</span></div>{distributionPoints.length === 0 ? <div className="mx-portfolio-empty-list"><span>Sin distribuciones registradas</span><small>La línea se construirá con los movimientos de tipo Distribución.</small></div> : <svg className="mx-line-chart" viewBox="0 0 640 150" role="img" aria-label="Distribuciones acumuladas">{distributionPoints.length > 1 && <polyline points={distributionPoints.map((item, index) => `${(index / (distributionPoints.length - 1)) * 620 + 10},${140 - (item.cumulative / maxDistribution) * 115}`).join(" ")} fill="none" stroke="#f3a712" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" />}{distributionPoints.map((item, index) => <circle key={item.id} cx={distributionPoints.length === 1 ? 320 : (index / (distributionPoints.length - 1)) * 620 + 10} cy={140 - (item.cumulative / maxDistribution) * 115} r="5" fill="#111113" stroke="#f3a712" strokeWidth="3" />)}</svg>}</section><section className="mx-portfolio-latest"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label"><CalendarDays size={14} />Actividad reciente</span><h2>Últimos movimientos</h2></div><span>{records.length} registros</span></div>{records.length === 0 ? <div className="mx-portfolio-empty-list"><span>Aún no hay movimientos</span><small>Usa “Agregar movimiento” para registrar una compra, distribución o ajuste.</small></div> : records.slice(0, 5).map(record => <div className="mx-portfolio-latest-row" key={record.id}><strong>{record.ticker}</strong><span>{record.movement}</span><span>{record.date}</span><b>${((record.movement === "Compra" ? 1 : record.movement === "Venta" ? -1 : 1) * (record.movement === "Compra" || record.movement === "Venta" ? record.shares * record.price + (record.commission || 0) : record.amount)).toLocaleString("es-MX", { maximumFractionDigits: 2 })}</b></div>)}</section></section><section className="mx-following-panel"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label"><Heart size={14} />FIBRAs que sigues</span><h2>Tu lista educativa</h2></div><span>{following.length} seguidas</span></div>{following.length === 0 ? <div className="mx-portfolio-empty-list"><span>Aún no sigues ninguna FIBRA</span><small>Ve al catálogo principal y toca el corazón transparente de una FIBRA para agregarla aquí.</small></div> : <div className="mx-following-list">{following.map(ticker => <div className="mx-following-row" key={ticker}><div><Heart size={15} fill="currentColor" /><strong>{ticker}</strong><span>{followingCatalog[ticker] || "FIBRA mexicana"}</span></div><div className="mx-following-market"><strong>{followingMarket[ticker]?.price || "—"}</strong><span className={followingMarket[ticker]?.change.startsWith("+") ? "mx-positive" : followingMarket[ticker]?.change.startsWith("−") ? "mx-negative" : ""}>{followingMarket[ticker]?.change || "—"}</span><small>Yield {followingMarket[ticker]?.yield || "—"}</small></div><button className="mx-add-button" onClick={() => openForm("Compra")}><Plus size={13} />Agregar posición</button></div>)}</div>}</section><section className="mx-portfolio-holdings"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label">Detalle de FIBRAs</span><h2>Posiciones registradas</h2></div><span>{fibraStats.length} FIBRAs</span></div>{fibraStats.length === 0 ? <div className="mx-portfolio-empty-list"><span>Sin posiciones registradas</span><small>Agrega una compra para ver títulos, costo promedio e importe.</small></div> : <div className="mx-holdings-table"><div className="mx-holdings-head"><span>Emisora</span><span>Títulos</span><span>Costo promedio</span><span>Importe</span><span>Valor referencia</span></div>{fibraStats.map(item => <div className="mx-holdings-row" key={item.ticker}><strong>{item.ticker}</strong><span>{item.shares.toLocaleString("es-MX")}</span><span>${item.shares ? (item.invested / item.shares).toFixed(2) : "0.00"}</span><span>${item.invested.toLocaleString("es-MX", { maximumFractionDigits: 2 })}</span><b>${item.invested.toLocaleString("es-MX", { maximumFractionDigits: 2 })}</b></div>)}</div>}</section><section className="mx-portfolio-import"><div><div className="mx-portfolio-section-label"><ReceiptText size={15} />Importar comprobante</div><h2>Agrega una compra o distribución</h2><p>Sube una imagen o PDF de referencia y confirma los datos manualmente. La lectura automática queda preparada para una futura integración.</p></div><div className="mx-portfolio-import-actions"><label className="mx-file-import-button"><Upload size={14} />Importar XLSX/CSV<input type="file" accept=".xlsx,.xls,.csv" onChange={event => handleImportFile(event.target.files?.[0])} /></label><button className="mx-add-button" onClick={() => openForm("Compra")}><ReceiptText size={14} />Cargar comprobante</button></div>{importError && <p className="mx-import-error">{importError}</p>}{importRows.length > 0 && <div className="mx-import-preview"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label">Vista previa revisable</span><h2>{importRows.length} filas listas para confirmar</h2></div><button className="mx-add-button mx-primary-action" onClick={confirmImport}><Check size={14} />Confirmar importación</button></div><div className="mx-import-preview-list">{importRows.slice(0, 6).map((row, index) => <div key={`${row.ticker}-${row.date}-${index}`}><strong>{row.ticker}</strong><span>{row.movement}</span><span>{row.date}</span><span>{row.shares ? `${row.shares} títulos` : `$${row.amount.toFixed(2)}`}</span></div>)}</div></div>}</section>
    {showForm && <section id="portfolio-form" className="mx-portfolio-form mx-portfolio-form-modal"><div className="mx-portfolio-form-head"><div><span className="mx-portfolio-section-label"><FileImage size={14} />{editingId ? "Editar posición" : "Agregar posición"}</span><h2>Datos de la posición</h2></div><button className="mx-icon-button" aria-label="Cerrar formulario" onClick={resetForm}><X size={16} /></button></div><div className="mx-portfolio-form-grid"><label>FIBRA<select value={draft.ticker} onChange={event => updateDraft("ticker", event.target.value)}>{portfolioOptions.map(item => <option key={item}>{item}</option>)}</select></label><div className="mx-transaction-type"><span>Tipo</span><div><button type="button" className={draft.movement === "Compra" ? "active buy" : "buy"} onClick={() => updateDraft("movement", "Compra")}>🟢 Compra</button><button type="button" className={draft.movement === "Venta" ? "active sell" : "sell"} onClick={() => updateDraft("movement", "Venta")}>🔴 Venta</button></div></div><label>Fecha<input type="date" value={draft.date} onChange={event => updateDraft("date", event.target.value)} /></label><label>Fol​​io / referencia<input value={draft.folio} onChange={event => updateDraft("folio", event.target.value)} placeholder="Opcional" /></label><label>Títulos (CBFIs)<input type="number" min="0" step="1" value={draft.shares || ""} onChange={event => updateDraft("shares", event.target.value)} placeholder="0" /></label><label>Precio promedio de compra (MXN)<input type="number" min="0" step="0.01" value={draft.price || ""} onChange={event => updateDraft("price", event.target.value)} placeholder="0.00" /></label><label>Precio actual observado<input type="number" min="0" step="0.01" value={draft.currentPrice || ""} onChange={event => updateDraft("currentPrice", event.target.value)} placeholder="Opcional" /></label><label>Comisión / gastos<input type="number" min="0" step="0.01" value={draft.commission || ""} onChange={event => updateDraft("commission", event.target.value)} placeholder="0.00" /></label><label>Intermediario<input value={draft.broker} onChange={event => updateDraft("broker", event.target.value)} placeholder="Ej. GBM" /></label><label>Estado de fuente<select value={draft.sourceStatus} onChange={event => updateDraft("sourceStatus", event.target.value as PortfolioRecord["sourceStatus"])}><option value="Pendiente">Pendiente de confirmar</option><option value="Confirmado">Confirmado por documento</option></select></label><label className={draft.movement === "Compra" || draft.movement === "Venta" ? "mx-field-conditional-hidden" : ""}>Monto del movimiento<input type="number" min="0" step="0.01" value={draft.amount || ""} onChange={event => updateDraft("amount", event.target.value)} placeholder="Para distribución o ajuste" /></label><label>Comprobante<input type="file" accept="image/*,.pdf" onChange={event => handleReceipt(event.target.files?.[0])} />{draft.receiptName && <small className="mx-file-name">{draft.receiptName}</small>}</label><label className="mx-field-wide">Notas (opcional)<textarea value={draft.notes} onChange={event => updateDraft("notes", event.target.value)} placeholder="Ej: compra en GBM" /></label></div><div className="mx-portfolio-form-actions"><button className="mx-add-button mx-primary-action" onClick={saveRecord}><Check size={14} />{editingId ? "Guardar cambios" : "Agregar"}</button><button className="mx-reset" onClick={resetForm}>Cancelar</button>{saved && <span className="mx-saved-message">Registro guardado en este navegador</span>}</div></section>}
    <section className="mx-portfolio-records"><div className="mx-portfolio-section-heading"><div><span className="mx-portfolio-section-label"><CalendarDays size={14} />Historial manual</span><h2>Movimientos de FIBRAs</h2></div><span>{records.length} registros</span></div>{records.length === 0 ? <div className="mx-empty-tool mx-portfolio-empty"><BriefcaseBusiness size={28} /><span>Tu portafolio educativo está vacío</span><small>Agrega una FIBRA, captura una compra o registra una distribución para comenzar.</small><button className="mx-add-button" onClick={() => openForm("Compra")}>Crear primer registro</button></div> : <div className="mx-portfolio-table"><div className="mx-portfolio-table-head"><span>Fecha</span><span>FIBRA</span><span>Movimiento</span><span>Detalle</span><span>Referencia</span><span>Acciones</span></div>{records.map(record => <div className="mx-portfolio-table-row" key={record.id}><span>{record.date}</span><strong>{record.ticker}</strong><span className="mx-movement-badge">{record.movement}</span><span>{record.movement === "Compra" || record.movement === "Venta" ? `${record.shares} títulos · ${record.movement === "Venta" ? "−" : ""}$${(record.shares * record.price + (record.commission || 0)).toFixed(2)}` : `$${record.amount.toFixed(2)}`}</span><span>{record.receiptName ? <><FileImage size={13} />{record.receiptName}</> : record.folio || `${record.broker || "Sin intermediario"} · ${record.sourceStatus}`}</span><span className="mx-row-actions"><button aria-label={`Editar ${record.ticker}`} onClick={() => editRecord(record)}><Pencil size={14} /></button><button aria-label={`Eliminar ${record.ticker}`} onClick={() => deleteRecord(record.id)}><Trash2 size={14} /></button></span></div>)}</div>} </section>
    <p className="mx-disclaimer">Este portafolio es un registro educativo local. Los datos se guardan solo en este navegador, no representan una cuenta de inversión y no sustituyen los documentos oficiales de tu intermediario.</p></main><Footer /></div>;
}
